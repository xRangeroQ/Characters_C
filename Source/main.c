// Libraries
#include <stdio.h>
#include <stdlib.h>

// Main Function
int main()
{
  // For Loop
  for (int ch = 0; ch < 256; ch++)
  {
    printf("Number: %d | Character: %c\n", ch, (char)ch);
  }

  // Return Function
  return 0;
}
